{
  //If this configuration is not in the bin directory, all plugins will be loaded.
  //Plugin configurations must be in appsetting.json file; do not configure them in other environments config json file.
  //Business modules loaded dynamically as plugins (the business module name is also the name of the assembly DLL)
  "Plugins": {
    "PluginsRoot": "Plugins",
    "DisableAutoload": false, //When true, it loads directories AssemblyNames. Otherwise, it loads all assemblies named in subdirectories under PluginsRoot.
    "AssemblyNames": {
      //Assembly name: Load or not?
      "JobSamples": true
    }
  }
}
